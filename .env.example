DATABASE_URL=postgresql://postgres:postgres@localhost:5433/cocos
# Hosted Neon database (challenge-provided credentials, never committed):
# DATABASE_URL=postgresql://<user>:<password>@<host>/<database>?sslmode=require
