meta {
  name: 200: la misma clave repite esa orden y no coloca nada
  type: http
  seq: 21
}

post {
  url: {{baseUrl}}/orders
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Idempotency-Key: 7f3a1c9e-buy-pamp-10
}

body:json {
  { "userId": 1, "instrumentId": 47, "side": "BUY", "type": "MARKET", "size": 10 }
}

assert {
  res.status: eq 200
}

docs {
  Comparte la clave con el caso anterior, que corre primero: mandá ese antes si corrés
  este suelto. El 200 es fijo, la orden ya existe y la API la devuelve tal cual en vez
  de decidir de nuevo.
}
